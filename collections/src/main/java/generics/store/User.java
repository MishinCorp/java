package generics.store;

/**
 * Class User.
 * Implements a User entity.
 *
 * @author Mishin Yura (mishin.inbox@gmail.com)
 * @since 17.10.2018
 */
public class User extends Base {
    /**
     * Constructor.
     *
     * @param pId ID.
     */
    protected User(final String pId) {
        super(pId);
    }
}

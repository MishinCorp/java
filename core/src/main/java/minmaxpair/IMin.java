package minmaxpair;

/**
 * Interface IMin.
 * Declares methods for getting min of the array.
 *
 * @author Mishin Yura (mishin.inbox@gmail.com)
 * @since 31.08.2018
 */
public interface IMin {
    /**
     * Method gets min of the array.
     *
     * @param anArray Array.
     * @return min.
     */
    int getMin(final int[] anArray);
}

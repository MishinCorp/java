package minmaxpair;

/**
 * Interface IMax.
 * Declares methods for getting max of the array.
 *
 * @author Mishin Yura (mishin.inbox@gmail.com)
 * @since 31.08.2018
 */
public interface IMax {
    /**
     * Method gets max of the array.
     *
     * @param anArray Array.
     * @return max.
     */
    int getMax(final int[] anArray);
}
